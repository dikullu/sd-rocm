# Copy this file to .env and customize the values below.
# WARNING: Do NOT commit your .env file. It contains machine-specific and/or sensitive settings.
# The repo's .gitignore is configured to ignore .env files.

# Python version used for the virtual environment
PYTHON_VERSION=3.12

# ROCm setup: choose one of [nightly, release, cpuonly]
ROCM_VERSION=nightly

# Instance name to separate environments/venvs
DOCKER_INSTANCE=local-comfyui

# Root directory for all generated data and virtual envs
# Tip: Use an absolute path. When running locally as non-root, run-local.sh will prevent using /root.
ROOT_DIR=/absolute/path/to/data/home-local

# ComfyUI server port
COMFYUI_PORT=31490

# (Optional) WebUI port, used by setup_webui/launch_webui paths
WEBUI_PORT=7860

# Non-interactive mode (1 to enable). When enabled, the prompts will be controlled via REINSTALL_* flags below
NON_INTERACTIVE=0

# Control reinstall prompts in non-interactive mode
REINSTALL_ROCM=n
REINSTALL_FLASH=n
